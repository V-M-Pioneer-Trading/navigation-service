introspection.json in this directory is a VERBATIM copy. Do not edit it here.

  original: meta/fixtures/introspection.json
            (V-M-Pioneer-Trading/meta, repo root `fixtures/introspection.json`)
  meta commit: 9b6274657743ec6f626f7d56e4a01069de7d43e0 (2026-09-25, fixture version 3, meta#88)
  sha256: 77f845c89d4baabef7a336325a9e30360d908fd761ad450904c693621547dfa3

The sha256 above is the hash of the LF bytes of introspection.json and is
asserted by IntrospectionConformanceTest, so the pin enforces itself instead of
relying on someone remembering to re-copy. The repository's .gitattributes
marks that one file `-text` so a Windows checkout (core.autocrlf=true) keeps
the same bytes a Linux checkout has and the hash holds on both.

The provenance lives in this sibling file rather than as a key inside the JSON
so the copy stays byte-identical to the original and `diff` is the drift check.
Change meta first, then re-copy and update the commit and the sha256 above.

navigation-service is a CALLING SERVICE. All 37 `cases` bind it and are driven
by IntrospectionConformanceTest through the real HandlerInterceptor and the
real java.net.http client against a real stub of the center (the JDK's
com.sun.net.httpserver). The 11 `gatewayCases` are st-gateway's lane policy,
which this service does not implement; the test skips them by name and asserts
there are exactly 11, so a gateway case added in meta is noticed here too.
